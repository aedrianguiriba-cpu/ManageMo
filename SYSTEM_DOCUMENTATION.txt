================================================================================
                     MANAGEMO — SYSTEM DOCUMENTATION
              Inventory & Asset Management System
              Pampanga State University (PSU)
              Version 2.0.0
================================================================================

OVERVIEW
--------
ManageMo is a web-based inventory and asset management system built for
Pampanga State University (PSU), plus a companion Flutter mobile app for
confirming deliveries by QR code.

The web app runs on PHP (no framework) using Bootstrap 5.3 and Font Awesome 6.4.
All pages are server-rendered PHP. Session-based authentication (BCrypt passwords).

Persistence is a REAL Supabase (hosted Postgres + PostgREST) database — this is
NOT a hardcoded/mock-data system. All reads/writes (requests, inventory, users,
borrow records, condemnation, etc.) go through the REST API and are durable
across requests. Data access lives in:
  config/data.php                — data-layer functions (getUsers, getInventory,
                                    getRequests, ...) backed by Supabase REST calls,
                                    with a short-lived in-process cache (60s TTL)
  config/supabase.php             — thin PostgREST HTTP client (select/insert/
                                     update/delete) + .env loader
  config/mock_data.php            — legacy MockData class kept for reference only;
                                     NOT called anywhere in the current codebase
  config/functions.php            — business logic helpers (auth, QR generation,
                                     email, dbCreate*/dbUpdate*/dbDelete* wrappers)
  config/constants.php            — app-wide constants (roles, statuses, urgency)
  config/departments_custom.json  — user-added colleges, offices, and campuses
  .env                             — SUPABASE_URL, SUPABASE_KEY (service_role),
                                     API_TOKEN_SECRET (used by api/*.php)

================================================================================
DIRECTORY STRUCTURE
================================================================================

ManageMo/
├── index.php                   Login page (split-panel design, blurred bg)
├── auth.php                    Login POST handler (authenticates, sets session)
├── logout.php                  Destroys session, redirects to login
├── signup.php                  User self-registration (currently disabled/mock)
├── forgot-password.php         Password reset placeholder
│
├── config/
│   ├── constants.php           App constants (roles, request/borrow statuses, etc.)
│   ├── data.php                Data layer — all getXxx() functions, Supabase-backed
│   ├── supabase.php             PostgREST HTTP client + .env loader
│   ├── functions.php            Business logic (auth, formatting, QR gen, email,
│   │                            dbCreate*/dbUpdate*/dbDelete* persistence helpers)
│   ├── mock_data.php            Legacy MockData class — unused, kept for reference
│   └── departments_custom.json  Persisted user-added colleges, offices, campuses
│
├── includes/
│   ├── header.php              HTML <head>, Bootstrap/FA CDN links (cache-busted style.css)
│   ├── navbar.php              Left sidebar navigation (role-aware links, active state)
│   ├── topbar.php              Top bar — page title + user dropdown
│   └── footer.php              Closing </body></html>
│
├── user/                       Pages accessible to Role: user
│   ├── dashboard.php           User home — KPI cards, request summary, recent activity
│   ├── inventory.php           Browse campus inventory + "My Owned Items" tab (paginated)
│   ├── requests.php            Submit new request (borrow / item / service)
│   ├── my-requests.php         View all submitted requests, grouped by unit (paginated)
│   ├── borrow-records.php      My Records — 3-tab page (Borrow/Item/Service), each paginated
│   └── settings.php            Profile/account settings (persisted via dbUpdateUser)
│
├── admin/                      Pages accessible to Role: admin
│   ├── dashboard.php           Admin home — KPI cards, charts, campus summary
│   ├── inventory.php           Inventory management (all campuses) — add/edit only,
│   │                           delete has been removed (see Module 2)
│   ├── inventory-campus.php    Campus overview + Manage Departments modal
│   ├── campus-detail.php       Single campus detail view
│   ├── requests.php            Review & approve/disapprove requests
│   ├── analytics.php           Charts and usage analytics
│   ├── reports.php             Printable reports
│   ├── condemnation.php        Condemnation & Disposal lifecycle management
│   ├── users.php               User management
│   └── settings.php            System/admin settings (persisted via dbUpdateUser)
│
├── api/                        JSON REST API for the Flutter mobile app (see Module 13)
│   ├── _bootstrap.php           Shared helpers: stateless signed-token auth, JSON I/O
│   ├── login.php                POST — verify email/password, issue a session token
│   ├── me.php                   GET  — resolve a token back to its user
│   ├── pending_deliveries.php    GET  — this user's out_for_delivery requests
│   └── confirm_delivery.php     POST — confirm one unit's delivery by qr_code_id
│   NOTE: the current mobile app does NOT call these — it talks to Supabase
│   directly instead (see Module 13's security note). These endpoints are kept
│   for any future client that should NOT hold the Supabase service_role key.
│
├── mobile_app/                 Flutter delivery-confirmation app (see Module 13)
│   ├── lib/
│   │   ├── main.dart            App entry — restores saved session or shows login
│   │   ├── screens/
│   │   │   ├── login_screen.dart     Email/password login (same accounts as web app)
│   │   │   ├── home_screen.dart      2 tabs: "Pending Scan" / "Completed"
│   │   │   └── scanner_screen.dart   Live QR scan, per-unit delivery confirmation
│   │   └── services/
│   │       ├── supabase_config.dart  Hardcoded Supabase URL + service_role key
│   │       ├── supabase_rest.dart    Thin PostgREST wrapper (mirrors config/supabase.php)
│   │       └── api_client.dart       Login (bcrypt check), pending/completed lists,
│   │                                 confirmDelivery() — mirrors admin "Mark Delivered"
│   ├── android/, ios/            Camera permission wired in AndroidManifest.xml / Info.plist
│   └── test/widget_test.dart     Smoke test — app boots to login screen
│
├── assets/
│   ├── pics/                   Logo and UI images (logo.png, storagebg.jpg)
│   ├── qrcodes/                Generated QR code images (PNG)
│   └── uploads/                File uploads
│
├── css/
│   └── style.css               Global stylesheet — sidebar, topbar, cards, forms
│                                Design tokens: --primary #8B0000, --border #e5e7eb
│
├── js/
│   └── script.js               Global JS (sidebar toggle, misc utilities)
│
└── lib/
    └── qrcode.php               QR code image generator library

================================================================================
MODULE DOCUMENTATION
================================================================================

────────────────────────────────────────────────────────────────────────────────
MODULE 1: AUTHENTICATION  (index.php, auth.php, logout.php)
────────────────────────────────────────────────────────────────────────────────
Flow:
  1. User visits index.php — shown split-panel login form (email + password).
     Left panel: storagebg.jpg with blurred backdrop + maroon gradient overlay.
     Right panel: white form panel with logo, inputs, remember me, forgot password.
  2. Form POSTs to index.php (self-posting).
  3. PHP calls getUsers(), finds user by email, verifies BCrypt hash via
     password_verify(). On success: sets $_SESSION['user_id'], redirects to
     role-appropriate dashboard (/admin/dashboard.php or /user/dashboard.php).
  4. Forgot Password form toggles in-place via showForgot()/showLogin() JS.
     No actual email is sent — placeholder success message shown.
  5. logout.php calls session_destroy(), redirects to index.php.

Role system:
  ROLE_ADMIN = 'admin'  → admin/ pages only
  ROLE_USER  = 'user'   → user/ pages only
  requireAdmin() / requireUser() guard every page and redirect if role mismatch.

Security:
  - Passwords hashed with BCrypt (PASSWORD_BCRYPT / $2y$ hashes).
  - All user-supplied values passed through sanitizeInput() (htmlspecialchars + trim).
  - The mobile app verifies the SAME bcrypt hash client-side with Dart's `bcrypt`
    package — confirmed cross-compatible with PHP's password_hash() output.

────────────────────────────────────────────────────────────────────────────────
MODULE 2: INVENTORY  (user/inventory.php, admin/inventory.php, admin/inventory-campus.php)
────────────────────────────────────────────────────────────────────────────────
Data source: getInventory() → array of items (live Supabase `inventory` table).
Each item has:
  id, qr_code_id, item_name, category, description,
  campus_id, college_id (Main Campus only), quantity, status, location,
  purchase_date, cost, condition, condemnation_reason, condemned_at,
  disposal_notes, disposed_at, group_id, created_at

Item statuses: available | borrowed | maintenance | damaged | requested | condemned | disposed

QR Code system:
  - Every inventory row has its own qr_code_id (e.g. 'QR-A1B2C3D4E5...'), one
    physical unit per row (adding N units = N inventory rows, not one row with
    quantity=N and per-unit suffixes).
  - lib/qrcode.php generates the actual QR image for each code.
  - QR images are stored in assets/qrcodes/.

User view:  read-only browse (Available/Borrowed tabs) + "My Owned Items" tab,
            both paginated (ITEMS_PER_PAGE=15 groups per page).
Admin view: add items, add units to an existing item, edit item details/status,
            view a per-item detail modal with each unit's QR code.
            Deletion has been REMOVED — there is no way to delete an inventory
            item from the admin UI or its POST handler anymore. To retire an
            item, use the Condemnation & Disposal flow (Module 9) instead.
Campus view (admin/inventory-campus.php):
  - Grid of all campus cards with per-campus stats.
  - Main Campus card shows Colleges and Offices breakdowns.
  - "Manage Departments" button opens a tabbed modal (see Module 12).

────────────────────────────────────────────────────────────────────────────────
MODULE 3: REQUEST SUBMISSION  (user/requests.php)
────────────────────────────────────────────────────────────────────────────────
Three request types (selected via type cards at the top):
  a) Borrow   — borrow an existing inventory item temporarily (catalog-only —
                see "Custom items" below)
  b) Item     — request procurement of a new item (catalog OR custom name)
  c) Service  — request maintenance/repair/service on an existing item

Form fields (vary by type):
  Borrow:  item (shop grid, catalog only), quantity, expected return date,
           urgency, reason
  Item:    item (shop grid, catalog OR "Other/Custom" with a free-text name),
           description, quantity, urgency, reason
  Service: item (shop grid), service type, description, urgency

Page layout: two-column (.rq-layout) — form card (left) + request summary
sidebar (right, 300px). Collapses to single column at ≤900px.

Custom items ("Item" request type only):
  - The Borrow flow's "Other (custom)" option has been removed — users can only
    borrow items that already exist in the catalog.
  - For an Item request with no catalog match, NO inventory row is created at
    submission time. The custom name is stashed in the request's
    service_description column so it still displays correctly everywhere
    (my-requests.php, admin/requests.php list + detail views) while pending.
  - An inventory row for the custom item is created ONLY when an admin approves
    the request (admin/requests.php's approve handler) — category defaults to
    "Other", campus = the requester's campus, condition defaults to "good"
    (a missing condition previously broke the admin item-detail modal's JS).
    The new inventory_id is then written back onto the request row so it's
    "counted" in inventory going forward.
  - Disapproved/still-pending custom item requests never get an inventory row.

Server-side:
  - Each unit of a submitted request becomes its own row in `requests` (one
    row = one physical unit), sharing a `group_id` across the whole submission
    so multi-unit requests can be tracked/approved/delivered together.
  - dbCreateRequest() / dbUpdateRequest() / dbUpdateInventory() persist directly
    to Supabase — nothing here is session-mocked.

────────────────────────────────
SHOP-STYLE ITEM SELECTION GRIDS
────────────────────────────────
All three request types use a shop-style card grid for item selection instead
of a plain <select> dropdown. Each grid has:
  - A hidden <select> (original ID kept for JS compatibility with handleCatalogChange,
    addToCart, updateSummary, resetStaging)
  - A search input (live filter)
  - Category filter pills (horizontal scrolling, wraps on mobile)
  - A card grid

Grid 1 — Borrow (#bshop-grid, $borrow_catalog):
  Includes available AND borrowed items. Cards display:
    - Category icon + color strip (.bshop-icon)
    - Item name (.bshop-name)
    - Location chip (.bshop-loc)
    - Availability badge: "Available (qty)" green or "X borrowed" amber
    - Expected return date pills (one per active borrow record, sorted asc)
    - Unit price + status pill in footer
  Catalog-only — no "Other/Custom" card (removed).
  selectBorrowCard() / filterShopItems() handle selection & filtering.
  data-item-id, data-status, data-quantity, data-available on each card.
  Category filter: filterShopItems(btn) toggles .bshop-cat.active.

Grid 2 — Item Request (#ishop-grid, $catalog_by_category):
  Available-only items, PLUS an "Other / Custom" card (see "Custom items" above).
  selectItemReqCard() / filterItemShop() handle selection & filtering.

Grid 3 — Service (#sshop-grid, $inventory_items):
  All campus items regardless of status. Status-colored badge per item.
  selectServiceCard() / filterServiceShop() handle selection & filtering.

JS function resetStaging() clears all three grids on request type switch.

Category color map ($catMeta in PHP, reused in all three grids):
  Electronics      icon fa-laptop             color #1e40af  bg #dbeafe
  Furniture        icon fa-chair              color #92400e  bg #fef3c7
  Equipment        icon fa-screwdriver-wrench color #166534  bg #dcfce7
  Supplies         icon fa-boxes-stacked      color #6b21a8  bg #f3e8ff
  Appliances       icon fa-plug               color #0f766e  bg #ccfbf1
  Security         icon fa-shield-halved      color #9f1239  bg #ffe4e6
  Office Equipment icon fa-print              color #0369a1  bg #e0f2fe
  (default)        icon fa-box                color #6b7280  bg #f3f4f6

PHP data ($borrow_catalog):
  - Includes BOTH available and borrowed items (not just available).
  - Items grouped by category.
  - $item_avail_data maps inventory_id → array of active borrow records,
    each with { return_date, is_overdue }. Multiple records exist when multiple
    units of the same item are borrowed simultaneously.

Quantity cap:
  - handleCatalogChange() reads data-available from the selected card/option
    and sets the quantity input's max attribute to (total qty − borrowed count).
  - If the current value exceeds the new max it is automatically clamped down.
  - Tooltip on the input shows "Maximum available: N".

─────────────────────────
ITEM AVAILABILITY CALENDAR
─────────────────────────
  - Shown only when a BORROWED item is selected in the borrow grid.
  - Hidden automatically when switching request types (Borrow/Item/Service).
  - Hidden when an available item is selected.

  Status bar (two-row layout):
    Row 1: clock icon + "X of Y units currently borrowed"
    Row 2: "Expected returns:" label + amber pill chips, one per return date
           (sorted ascending, wrap-friendly).

  Calendar header: maroon "AVAILABILITY" title (left) + month nav buttons (right).
  Calendar grid:
    - iac-past    — past days (muted grey)
    - iac-unavail — future days before the last return date (red, strikethrough)
    - iac-returns — each expected return date (amber gradient, border, ↵ indicator)
    - iac-avail   — days after all items returned (green fill)
    - iac-today   — today ring (maroon outline); overrides fill when applicable

  Legend pills at bottom: Unavailable | Returns | Available

  Navigation: iacNav(dir) moves one month; auto-navigates to earliest return month
  on item select. itemAvailData JS variable holds the PHP-generated $item_avail_json.

  CSS classes: .iac-outer, .iac-bar, .iac-bar-top, .iac-bar-chips, .iac-ret-chip,
               .iac-bar-borrowed, .iac-cal-header, .iac-cal-title, .iac-cal-nav,
               .iac-cal-panel, .iac-grid, .iac-dow, .iac-cell, .iac-legend,
               .iac-leg-item, .iac-leg-dot

────────────────────────────────────────────────────────────────────────────────
MODULE 4: REQUEST MANAGEMENT / APPROVAL  (admin/requests.php)
────────────────────────────────────────────────────────────────────────────────
Data source: getRequests() → array of all requests (live Supabase `requests` table).
Each request row has:
  id, request_number, user_id, group_id, qr_code_id, request_type (borrow/item/
  service), inventory_id (nullable — see Module 3's "Custom items"),
  service_description, urgency (low/medium/high/critical), receiving_method,
  reason_for_request, expected_return_date, quantity_requested, status,
  delivery_status, approved_by, approved_at, created_at, updated_at

Request lifecycle:
  pending → approved / disapproved → (approved) → out_for_delivery → delivered
                                                                    → (borrow) returned

Admin actions (grouped — one action applies to every unit sharing a group_id):
  - Filter by type, status, urgency, campus; table view or tracker (stepper) view.
  - Approve → status='approved'. If the request has no inventory_id (a custom
    item — see Module 3), an inventory row is created for it HERE and linked
    back onto the request before the status change is applied.
  - Disapprove with reason → status='disapproved'; releases any reserved
    inventory unit back to 'available'.
  - Mark Out for Delivery → delivery_status='out_for_delivery' (+ email).
  - Mark Delivered → delivery_status='delivered', status='delivered'; for borrow
    requests, flips the inventory unit to 'borrowed' and creates a borrow_record.
    (The mobile app's QR scan performs this same update per-unit instead of the
    admin doing it manually — see Module 13.)

Item name display:
  - The item/service name is resolved from the linked inventory row, falling
    back to service_description (covers custom items and legacy rows with no
    request_items match) before finally falling back to "N/A"/"Unknown Item".
  - Shown in: the dedicated Item/Service tab tables, the "All" tab's table view
    (added "Item" column), and the tracker card view (added under the
    requester's name).

Email notifications:
  sendDeliveryEmail($email, $name, $request_number, $stage)
    stages: 'out_for_delivery' | 'pickup_ready'
  Uses PHP mail() — requires a working SMTP/sendmail config.

────────────────────────────────────────────────────────────────────────────────
MODULE 5: MY RECORDS  (user/borrow-records.php)
────────────────────────────────────────────────────────────────────────────────
Three tabs via ?tab=borrow|item|service (default: borrow).
Status filter per tab via ?status= (hidden input preserves tab on form submit).
Each tab is paginated independently at 10 rows/page (shared `page` query param —
safe because only one tab's table is ever rendered per request).

Tab 1 — Borrow Records:
  Source: getBorrowRecords() filtered by user_id, plus any borrow requests not
  yet delivered (shown as "in progress" rows with no borrow_records entry yet).
  Columns: Item, Borrowed, Expected Return, Returned On, Status, Notes.
  Status badges: active (blue) | returned (green) | overdue (red) | pending (amber).
  Overdue detection: active record where expected_return_date < now().

Tab 2 — Item Requests:
  Source: getRequests() filtered by user_id + request_type='item'.
  Columns: Request #, Item/Description, Urgency, Reason, Submitted, Status.

Tab 3 — Service Requests:
  Source: getRequests() filtered by user_id + request_type='service'.
  Columns: Request #, Item, Service Type, Description, Urgency, Submitted, Status.

Print: window.print() — @media print hides sidebar/topbar/tabs/filters/pagination.

────────────────────────────────────────────────────────────────────────────────
MODULE 5b: MY REQUESTS  (user/my-requests.php)
────────────────────────────────────────────────────────────────────────────────
One card per submitted request group (multi-unit requests collapse into a single
card with a stepper showing Submitted → Under Review → Approved/Disapproved →
Out for Delivery → Delivered). Filterable by status and type via the stat
cards / type pills at the top.

Paginated at 5 cards per page, with prev/next + numbered page links and a
"Showing X–Y of Z requests" counter. Filters (status/type) are preserved across
page links; the empty state still offers a "Clear filters" link.

────────────────────────────────────────────────────────────────────────────────
MODULE 6: ADMIN DASHBOARD  (admin/dashboard.php)
────────────────────────────────────────────────────────────────────────────────
Sections (top to bottom):
  1. KPI Cards — Total Items, Available, Borrowed, Maintenance, User-Owned Items.
     Each card has a full colored border matching the metric color.

  2. Campus Inventory Overview (Line Chart)
     Chart.js 4.4 line graph — one line per metric (Total/Borrowed/Maintenance)
     across all campuses. Smooth curves, filled areas.

  3. Campus Summary Table
     Per-campus row: Total, Borrowed, Requested, Maintenance counts + View button
     that opens the campus detail modal.

  4. Charts Row (3 columns):
     - Item Status Doughnut  — available / borrowed / maintenance breakdown
     - Request Status Doughnut — pending / approved / disapproved + stat rows
       + "Review All Requests" button
     - Quick Actions — 6 shortcut buttons to key admin pages

  5. Recent Requests + Recently Added Items
     Feed of the 5 most recent entries for each.

────────────────────────────────────────────────────────────────────────────────
MODULE 7: USER DASHBOARD  (user/dashboard.php)
────────────────────────────────────────────────────────────────────────────────
Sections (top to bottom):
  1. Welcome Banner — maroon banner with campus name, date, "New Request" button.

  2. KPI Cards — Campus Items, Available, Active Borrows, Pending, My Owned Items.
     White background with colored border per card.

  3. Return Schedule Calendar (hero section, col-lg-8) + Sidebar (col-lg-4)
     The calendar is the main visual focus of the dashboard.

     Calendar (#fullCal):
       - Pure-JS monthly calendar (no third-party library).
       - Cells are 80px tall; up to 2 event chips per day + overflow indicator.
       - Event chips (fc-ev-chip) are color-coded by category:
           Electronics    #dbeafe / #1e40af
           Furniture      #fef3c7 / #92400e
           Equipment      #dcfce7 / #166534
           Supplies       #f3e8ff / #6b21a8
           Appliances     #ccfbf1 / #0f766e
           Security       #ffe4e6 / #9f1239
           Office Equipment #e0f2fe / #0369a1
       - Today highlighted with maroon circle (fc-today-num).
       - Clicking a day highlights it (fc-selected, maroon outline) and updates the
         sidebar to list items returning on that date.
       - Navigation: fcNav(dir), fcGoToday() global JS functions.
       - Data source: $cal_events_json (PHP) — map of YYYY-MM-DD → [{name, category}]
         built from active/overdue borrow records with future expected_return_dates.

     Sidebar (#fcSidebar + #fcLegend):
       - Title updates to show selected date or "Select a date".
       - Lists all items returning on the selected day with category badges.
       - Category color legend at the bottom.

  4. Quick Actions | Request Summary | Campus Inventory Breakdown (3 columns).

  5. Browse & Reserve Items (full-width, col-12):
     - Category filter buttons at the top.
     - Item cards show: name, category badge, available quantity (green if > 0),
       borrowed return date chip (blue, if currently borrowed), Reserve button
       (maroon, links to requests.php?item_id=X).
     - filterIbItems(cat, btn) JS function handles category filtering.
     - $campus_inventory filtered to current user's campus_id.

  6. Recent Requests table + Campus Items list.

  7. My Owned Items list.

  PHP data computation (top of file):
    $item_return_map — maps inventory_id → earliest active/overdue expected_return_date
    $cal_events      — maps YYYY-MM-DD   → [{name, category}] for calendar rendering
    $cal_events_json — JSON-encoded $cal_events passed to JS

────────────────────────────────────────────────────────────────────────────────
MODULE 8: BORROW MANAGEMENT  (implicit via borrow-records + admin/requests)
────────────────────────────────────────────────────────────────────────────────
Data source: getBorrowRecords() → array of borrow tracking records.
Each record has:
  id, inventory_id, user_id, request_id,
  borrow_date, expected_return_date, actual_return_date (null until returned),
  status (active/returned/overdue), notes, created_at

Overdue logic: status='active' AND expected_return_date < today.
Return process: admin sets actual_return_date → status = 'returned' →
                inventory item status reverts to 'available'.
Delivery confirmation (creates the borrow_record in the first place):
  either the admin's "Mark Delivered" action, or the requester scanning the
  unit's QR code in the mobile app (Module 13) — both run the same logic.

Multi-unit borrows:
  A single item (e.g. one item_name with 5 inventory rows) may have multiple
  simultaneous active borrow records — one per borrowed unit. The borrowed
  count = number of active records for that item's inventory rows. Available
  count = total units − borrowed count.

────────────────────────────────────────────────────────────────────────────────
MODULE 9: CONDEMNATION & DISPOSAL  (admin/condemnation.php)
────────────────────────────────────────────────────────────────────────────────
Admin-only page for managing the end-of-life lifecycle of inventory items.
All mutations here are REAL, persisted writes to the `inventory` table via
dbUpdateInventory() — there is no session-override/mock layer.

Three tabs (via ?tab=evaluate|condemned|disposed, default: evaluate):
  Tab 1 — For Evaluation: EVERY inventory item not already condemned or
          disposed (any status/condition) — so an admin can condemn any
          specific item, not just ones already flagged damaged/maintenance.
  Tab 2 — Condemned:      items with status = 'condemned'
  Tab 3 — Disposed:       items with status = 'disposed'

KPI cards (top of page):
  For Evaluation  — amber  — count of items available to condemn
  Condemned       — maroon — count of condemned items
  Disposed        — grey   — count of disposed items
  Est. Value at Risk — red — sum of cost for all evaluate-tab items

Filter bar (below tab nav, submits via GET):
  - Campus dropdown (all campuses)
  - Category dropdown
  - Search text input (filters item name + QR code + category)
  Filters persist across tab switches via hidden <input name="tab">.

Actions per item (POST, self-posting to condemnation.php):
  action=condemn  → status='condemned', condemnation_reason, condemned_at (required reason)
  action=dispose   → status='disposed', disposal_notes (optional), disposed_at
  action=restore   → status='damaged', clears condemnation/disposal fields —
                     returns the item to the evaluation list
  All POST actions redirect via redirectWithMessage() to prevent form resubmission.

CSS namespace: all classes prefixed with .cd-*
  .cd-kpi-row, .cd-kpi-card, .cd-kpi-icon, .cd-kpi-val, .cd-kpi-lbl
  .cd-tab-nav, .cd-tab-link, .cd-tab-pane
  .cd-filter-card, .cd-filter-label
  .cd-table-card, .cd-table

────────────────────────────────────────────────────────────────────────────────
MODULE 10: ANALYTICS  (admin/analytics.php)
────────────────────────────────────────────────────────────────────────────────
Charts and metrics including:
  - Requests over time (by type)
  - Inventory status distribution (available/borrowed/maintenance)
  - Top requested items
  - Campus activity breakdown
  - Overdue rate

────────────────────────────────────────────────────────────────────────────────
MODULE 11: REPORTS  (admin/reports.php)
────────────────────────────────────────────────────────────────────────────────
Printable tabular reports:
  - Inventory by campus / category / status
  - Requests summary by date range
  - Borrow records with overdue flag
  - User activity log

────────────────────────────────────────────────────────────────────────────────
MODULE 12: DEPARTMENT & CAMPUS MANAGER  (admin/inventory-campus.php)
────────────────────────────────────────────────────────────────────────────────
Accessed via "Manage Departments" button on the Campus Inventory page.
Opens a tabbed modal with three tabs:

  Tab 1 — Colleges (Main Campus only)
    - Lists all colleges (8 defaults + any custom).
    - Add: enter abbreviation (auto-uppercased) + full name → POST → saved to JSON.
    - Delete: custom entries only; default colleges are protected.

  Tab 2 — Offices (Main Campus only)
    - Lists all administrative offices (14 defaults + any custom).
    - Add/delete same as colleges tab.

  Tab 3 — Campuses
    - Lists all PSU campuses (8 defaults + any custom).
    - Add: enter name, location, optional description → assigned auto-incremented ID.
    - Delete: custom campuses only; default 8 campuses are protected.

  Note: Colleges and Offices apply to Main Campus (ID 1) only.
        Other campuses do not have sub-department breakdowns.

Persistence:
  Custom entries are saved to config/departments_custom.json:
  {
    "colleges": { "CON": "College of Nursing (CON)" },
    "offices":  { "OAR": "Office of Alumni Relations (OAR)" },
    "campuses": [
      { "id": 9, "name": "Magalang Campus", "location": "Magalang, Pampanga",
        "description": "", "colleges": [] }
    ]
  }

  At runtime, getMainCampusColleges(), getMainCampusOffices(), and getCampuses()
  merge defaults with custom JSON entries automatically.

────────────────────────────────────────────────────────────────────────────────
MODULE 13: QR DELIVERY SCANNER  (mobile_app/ — Flutter app, IMPLEMENTED)
────────────────────────────────────────────────────────────────────────────────
A companion Flutter app that lets a user scan the QR code on a delivered item
to confirm receipt, instead of an admin manually clicking "Mark Delivered".

Architecture (IMPORTANT — read before extending this app):
  The app talks DIRECTLY to Supabase's PostgREST API using the SAME
  SUPABASE_KEY as the web app's .env — which is the service_role key. This
  key bypasses Row Level Security entirely and grants full read/write access
  to every table in the database.

  This was a deliberate, explicitly-confirmed tradeoff (the user chose this
  over a safer anon-key + Postgres-RPC design, and over routing through the
  api/*.php backend) — see mobile_app/lib/services/supabase_config.dart for
  the in-code warning. Practical consequence: anyone who installs/decompiles
  this app can extract the key and get full database access. If this app is
  ever distributed beyond a trusted/internal audience, this should be revisited
  — either move back to the api/*.php endpoints (key stays server-side only)
  or swap to a restricted anon key + RLS policies / SECURITY DEFINER RPCs.

  The api/*.php endpoints (Module "API" in the directory tree) already implement
  the safer alternative (stateless signed tokens, password check + all writes
  happen server-side) but are currently unused by the shipped app.

Login:
  - Same accounts as the web app (users.email / users.password).
  - App fetches the `users` row by email via PostgREST, then verifies the
    password with the `bcrypt` Dart package against the stored $2y$ bcrypt
    hash — confirmed byte-for-byte compatible with PHP's password_hash().
  - No password ever leaves the device in plaintext; only the fetched hash is
    compared locally. Session = the logged-in user's profile cached in
    SharedPreferences (there is no separate token — the embedded API key
    itself is what actually grants backend access).

Home screen — two tabs:
  - "Pending Scan": this user's requests with delivery_status='out_for_delivery',
    grouped by group_id (one card per request, showing item name(s) + unit count).
    Tapping a card opens the scanner SCOPED to that item (no generic scan button —
    the user must pick the item first).
  - "Completed": this user's delivery_status='delivered' requests, most recent first.
  Pull-to-refresh reloads both tabs.

Scanning flow (per-unit confirmation):
  - Only QR codes belonging to the SELECTED item's request/group are accepted;
    scanning an unrelated QR is rejected ("This QR code belongs to a different
    item...") rather than silently confirming the wrong thing.
  - Multi-unit requests are confirmed ONE QR AT A TIME — scanning one unit's
    QR does NOT auto-confirm the whole group. The scanner stays open between
    scans, shows a running "confirmed X of Y" progress banner, and prompts for
    the next unit's QR.
  - When the last unit of the selected item is confirmed, the scanner
    auto-closes (after a short delay) with a summary; the user can also tap
    "Done" at any time to leave early.
  - Confirming a unit mirrors admin/requests.php's "Mark Delivered" action:
    sets delivery_status/status='delivered' on that request row, and — for
    borrow-type requests — flips the inventory unit to 'borrowed' and inserts
    a borrow_records row.

QR Code format: whatever qr_code_id was generated for that inventory row /
request unit (see Module 2) — matched via an exact `qr_code_id=eq.` lookup,
no special per-unit suffix parsing needed.

Build/verify notes: `flutter analyze`, `flutter test`, and `flutter build apk`
all pass. Requires the CAMERA permission (wired into AndroidManifest.xml /
Info.plist's NSCameraUsageDescription).

────────────────────────────────────────────────────────────────────────────────
MODULE 14: USER MANAGEMENT  (admin/users.php)
────────────────────────────────────────────────────────────────────────────────
Admin-only. Lists all users (getUsers()).
Actions: view profile, activate/deactivate, assign campus/college.
Passwords are never displayed (BCrypt hash only).

────────────────────────────────────────────────────────────────────────────────
MODULE 15: SETTINGS  (user/settings.php, admin/settings.php)
────────────────────────────────────────────────────────────────────────────────
User settings: update full_name, phone, password (with current password verify).
Admin settings: system name, email, campus configuration.
Both persist via dbUpdateUser() — real Supabase writes, not session-mocked.

────────────────────────────────────────────────────────────────────────────────
MODULE 16: MOBILE RESPONSIVENESS  (css/style.css + per-page <style> blocks)
────────────────────────────────────────────────────────────────────────────────
The entire site is responsive and tested on mobile (≥320px) and tablet (≥576px).

Viewport meta tag (includes/header.php):
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

Sidebar behavior on mobile (≤991px):
  - Sidebar is always a full-screen OVERLAY — it never pushes content.
  - Default state: sidebar hidden (position:fixed, transform:translateX(-100%)).
  - Hamburger button (fixed top-left, z-index 1100) appears when sidebar is closed.
  - Clicking hamburger → sidebar slides in from left, dark backdrop overlay shows.
  - Clicking X or backdrop → sidebar closes, overlay dismissed.
  - The base CSS handles this correctly via .sidebar.collapsed { translateX(-100%) }.
    No mobile-specific CSS override is needed for the sidebar transform — the same
    JS (setSidebarOpen) and CSS rules apply on all screen sizes.

Key breakpoints in css/style.css:
  ≤991px (tablet/mobile):
    - .main-wrapper { margin-left: 0 !important } — no sidebar push
    - .topbar always at left:0 — sidebar overlays on top via z-index
    - .topbar padding-left: 58px — clears the fixed hamburger button
    - .main-wrapper.sidebar-collapsed padding-left/right: 20px
      (replaces the desktop icon-sidebar 60px padding)
    - .row > .col.min-width-0: flex: 0 0 50% — stat cards wrap to 2-per-row
    - Modal dialogs: max-width capped at viewport - 32px

  ≤575px (phones):
    - .main-wrapper padding-left/right: 12px
    - .topbar-user-info, .topbar-caret hidden (avatar only visible)
    - .topbar-title font-size reduced
    - Tables: smaller font (0.68rem th, 0.82rem td)
    - Modals: max-width calc(100vw - 20px) with 10px auto margins
    - Dashboard calendar cells: min-height 54px, fonts reduced

Per-page responsive rules (in each file's <style> block):
  user/requests.php:
    - .rq-type-grid: 1 column at ≤600px
    - #receiving_method_grid: 1 column at ≤600px
    - .rq-layout: 1 column at ≤900px (form + summary sidebar stack)
    - .urgency-group: 2 columns at ≤500px
    - .bshop-grid: 2 columns at ≤900px; 1 column at ≤560px

  user/dashboard.php:
    - .ud-welcome-banner: column layout at ≤576px
    - .fc-cell: min-height 54px, fonts reduced at ≤576px

  user/inventory.php + admin/inventory.php:
    - Inline 1fr 1fr detail grids: stack to 1fr via attribute selector at ≤576px

  admin/requests.php:
    - .ar-sticker-grid: 2 columns at ≤700px; 1 column at ≤480px

  admin/campus-detail.php:
    - .cd-stat-grid, .cd-inventory-summary: 1 column at ≤480px

  admin/dashboard.php:
    - Campus modal .campus-stat-row: 1 column at ≤400px

  admin/condemnation.php:
    - .cd-kpi-row: flex-wrap; each card min-width:180px (auto-wraps)
    - .cd-filter-card: flex-wrap (filters stack on small screens)

  admin/analytics.php, admin/users.php, admin/inventory-campus.php,
  admin/settings.php: all have their own media queries in their <style> blocks
  collapsing grids at 540–900px breakpoints.

================================================================================
DATA RELATIONSHIPS
================================================================================

Users ──────────────────── campus_id   →  Campuses
Users ──────────────────── college_id  →  Main Campus Colleges (string key)
Inventory ──────────────── campus_id   →  Campuses
Inventory ──────────────── college_id  →  Main Campus Colleges (Main Campus only)
Requests ───────────────── user_id     →  Users
Requests ───────────────── inventory_id → Inventory (nullable — see Module 3)
Requests ───────────────── approved_by  → Users (admin)
BorrowRecords ──────────── user_id     →  Users
BorrowRecords ──────────── inventory_id → Inventory
BorrowRecords ──────────── request_id   → Requests

================================================================================
REQUEST STATUS FLOW
================================================================================

                    ┌─────────┐
          Submit    │ pending │
         ─────────► │         │
                    └────┬────┘
                         │ Admin reviews
              ┌──────────┼──────────┐
              ▼                     ▼
       ┌──────────┐          ┌─────────────┐
       │ approved │          │ disapproved │ (terminal)
       └────┬─────┘          └─────────────┘
            │
            │ Marked out for delivery
            ▼
     ┌──────────────────┐
     │ out_for_delivery  │
     └────────┬──────────┘
              │ Admin's "Mark Delivered" OR user scans QR in mobile app
              ▼
       ┌───────────┐
       │ delivered │ (terminal for item/service requests)
       └───────────┘
            │ (for borrow requests only)
            ▼
       ┌──────────┐
       │ returned │ (terminal)
       └──────────┘

================================================================================
BORROW STATUS FLOW
================================================================================

  active ──► returned   (when actual_return_date is set)
  active ──► overdue    (auto-detected: expected_return_date < now AND no return)

================================================================================
URGENCY LEVELS
================================================================================

  low      — Not time-sensitive
  medium   — Standard priority (default)
  high     — Needed soon
  critical — Immediate attention required

================================================================================
CAMPUS STRUCTURE
================================================================================

Default campuses (cannot be deleted):
  ID 1 — Main Campus (Brgy. Cabambangan, Bacolor, Pampanga)
           Colleges: CEA, COE, CCS, CBS, CAS, CIT, CHTM, CSSP
           Offices:  OUP, OVPAA, OVPAF, OVPRDE, OUR, OSAS, HRMO,
                     ICTO, FBO, PMO, PPMO, ULib, GCC, PDO
  ID 2 — Mexico Campus
  ID 3 — Porac Campus
  ID 4 — Santo Tomas Campus
  ID 5 — Lubao Campus
  ID 6 — Candaba Campus
  ID 7 — Apalit Campus
  ID 8 — City of San Fernando Campus

Custom campuses are added via the Department Manager UI and stored in
config/departments_custom.json with auto-incremented IDs starting at 9.

Only Main Campus (ID 1) supports College and Office sub-department breakdowns.

================================================================================
DESIGN SYSTEM
================================================================================

Primary color:  #8B0000 (maroon)
Dark variant:   #6B0000
Border:         #e5e7eb
Background:     #ffffff
Muted bg:       #f7f7f7
Text:           #111111

CSS variables (defined in css/style.css):
  --primary, --primary-dk, --border, --bg, --bg-muted, --text, --text-2, --text-3

Design rules:
  - No gradients or glassmorphism on internal pages
  - Border-radius: max 8px (cards), 6px (buttons/inputs), 4px (badges)
  - Sidebar active state: background #8B0000 !important, color #fff !important
  - KPI cards: white background, full colored border (1.5px) matching metric color
  - Flat icons only — no icon wrapper backgrounds
  - Login page: split-panel, left = image + maroon gradient overlay, right = white form
  - Cache-busted style.css via ?v=filemtime() in header.php
  - The Flutter app reuses the same maroon (#8B0000) as its primary accent, with
    green (#15803D) for "confirmed/completed" states, for visual consistency
    with the web app.

================================================================================
TECH STACK
================================================================================

  Backend:      PHP 7.4+ (no framework, no ORM)
  Database:     Supabase (hosted Postgres, accessed via PostgREST REST API)
  Auth:         PHP sessions + BCrypt (password_hash / password_verify)
  Frontend:     Bootstrap 5.3, Font Awesome 6.4
  Charts:       Chart.js 4.4 (CDN) — line, doughnut chart types
  QR Writer:    lib/qrcode.php (PHP QR Code generator)
  Mobile App:   Flutter 3.x / Dart — packages: mobile_scanner (camera QR reading),
                http, shared_preferences, bcrypt (client-side hash verification)
  API (unused by the app, available for future clients):
                api/*.php — stateless HMAC-signed token auth, JSON over HTTPS
  Server:       Apache (XAMPP locally) — http://localhost/ManageMo/

================================================================================
KNOWN LIMITATIONS / OPERATIONAL NOTES
================================================================================

  - Inventory items cannot be deleted from the admin UI by design (Module 2) —
    use Condemnation & Disposal to retire an item instead.
  - Custom item requests submitted BEFORE the service_description fallback was
    added have no recoverable name (it was never persisted anywhere) and will
    still show "N/A"/"Custom Request". New submissions do not have this issue.
  - Email delivery (approval/out-for-delivery notices) requires a working PHP
    mail() / SMTP configuration — not verified/configured in this environment.
  - QR scanning in the mobile app requires camera permission and a physical/
    emulated camera; the emulator's virtual camera can scan codes shown on
    another screen or via its "extended controls" camera image injection.
  - SECURITY: the Flutter app embeds the Supabase service_role key directly
    (see Module 13) — this is a known, explicitly-accepted risk for the
    current trusted/internal deployment, not an oversight. Revisit before any
    wider distribution of the app.
  - config/mock_data.php is legacy/dead code — no longer referenced anywhere;
    kept only as a historical reference for the original data shape.

================================================================================
